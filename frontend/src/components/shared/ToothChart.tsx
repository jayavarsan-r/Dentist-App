// Replaced grid-based chart with the anatomical SVG version (spec section 8).
// Re-exported so existing imports of '@/components/shared/ToothChart' keep working.
export { default } from './ToothChartSVG';
